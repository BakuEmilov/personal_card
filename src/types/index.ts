export type IHeaderPerson = {
  profileImage: string;
  surName: string;
  name: string;
  middleName: string;
};

export type ILeftData = {
  gender: string;
  dateOfBirth: string;
  phoneNumber: string;
  email: string;
};

export type IRightData = {
  inn: string;
  passportNumber: string;
  dateOfIssue: string;
  endDate: string;
};
